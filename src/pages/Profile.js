




/*

const expirationDate = new Date(video.expiration_date);
const daysRemaining = Math.ceil((expirationDate - new Date()) / (1000 * 60 * 60 * 24));
if (daysRemaining <= 7) {
    alert('Ez a videó hamarosan lejár!');
}
 */